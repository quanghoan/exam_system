<% provide(:title, 'subject') %>
<h1> "subject"</h1>	
<div class="row">
	<div class="col-md-10 col-md-offset-1">
		<%= link_to "add question", new_question_path, class: "btn btn-success" %>
		<%= link_to "create test", controller: 'tests', action: :create, method: :post %>
		<table border="1" style="width:100%"> 
		  <tr>
		    <th>index</th>
		    <th>question</th>		
		    <th> answers</th>
		    <th>subject</th>
		    <th>action</th>
		  </tr>
		  <% @questions.each.with_index(1) do |question, index| %>
		  <tr>
		    <td><%= "#{index}" %></td>   	
		    <td><%= question.content %></td>
		    <td>
		    	<ul>
		    		<% question.answers.each do |answer| %>
		    			<% if answer.correct_answer %>
		    				<div id="correct" >
		    					<li><%= answer.content %></li>
		    				</div>
		    			<% else %>
		    				<%= answer.content %>
		    			<% end %>	
		    		<% end %>	
		    	</ul>
		    </td> 
		    <td><%=question.subject.title  %></td>
		    <td><%= link_to "Edit",edit_question_path(question) %>
		  			| <%= link_to "Delete", question, method: :delete, data: { confirm: "You sure?" } %>
		  	</td> 
		  </tr> 
		  <% end %>
		</table>
	</div>
</div>	